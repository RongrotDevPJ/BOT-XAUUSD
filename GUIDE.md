# คู่มือการใช้งาน XAUUSD Trading Bot (ฉบับสมบูรณ์)

ยินดีต้อนรับสู่ระบบเทรดทองคำอัตโนมัติ **Sniper Mode (M15 Trend Follow)**
ตอนนี้ระบบถูกจัดระเบียบให้เป็น **Microservice Structure** แล้ว ใช้ง่ายและเป็นระเบียบกว่าเดิมครับ!

---

## 📂 โครงสร้างโฟลเดอร์ (Project Structure)
เพื่อให้ดูง่าย เราแบ่งไฟล์เป็นหมวดหมู่ตามนี้ครับ:
*   **`config/`**: เก็บการตั้งค่า (ไปแก้ที่นี่)
*   **`logs/`**: เก็บประวัติการทำงานของบอท (Log File)
*   **`data/`**: เก็บประวัติการเทรดที่ส่งออก (CSV)
*   **`utils/`**: เก็บเครื่องมือเสริม (Data Tool)
*   **`app/`**: เก็บโค้ดหลักของบอท (ไม่ต้องยุ่งก็ได้ครับ)

---

## 🚀 1. วิธีเริ่มใช้งาน (Start)

**วิธีที่ง่ายที่สุด:**
1.  เข้าไปที่โฟลเดอร์โปรแกรม
2.  ดับเบิ้ลคลิกไฟล์ชื่อ **`run_bot.bat`**
3.  หน้าจอสีดำจะเด้งขึ้นมา... ให้ปล่อยทิ้งไว้ **ห้ามปิด!** (นั่นคือบอทกำลังทำงาน)

**วิธีแบบโปร:**
1.  เปิด Command Prompt (CMD) ในโฟลเดอร์นี้
2.  พิมพ์: `python main.py`

---

## 📊 2. การดูผลงาน (Monitoring)

### หน้าจอ Log (Command Prompt)
จะแสดงข้อความเมื่อมีเหตุการณ์สำคัญเท่านั้น:
-   `✅ Connected`: เชื่อมต่อสำเร็จ
-   `🔥 Signal: BUY/SELL`: กำลังเข้าออเดอร์
-   `📝 History Saved`: ปิดออเดอร์แล้ว (กำไร/ขาดทุน)

### ไฟล์ประวัติการเทรด (`data/trade_history.csv`)
ไฟล์นี้จะอยู่ในโฟลเดอร์ **`data`** ครับ
-   เปิดเข้าไปดูไฟล์ **`trade_history.csv`** ด้วย Excel ได้เลย

### ไฟล์บันทึกระบบ (`logs/trading.log`)
ถ้าอยากดูย้อนหลังว่าบอททำอะไรไปบ้าง ให้ไปที่โฟลเดอร์ **`logs`** แล้วเปิดไฟล์ **`trading.log`** ครับ

---

## 🛠 3. เครื่องมือดูดข้อมูล (`data_tool.py`)

ถ้าอยากดึงข้อมูลประวัติทั้งหมด หรือ อยากได้กราฟย้อนหลังไปทำ Backtest:
1.  **ปิดบอทตัวหลักก่อน** (Ctrl+C)
2.  พิมพ์คำสั่ง:
    ```bash
    python utils/data_tool.py
    ```
3.  เลือกเมนู:
    -   กด **1**: ดึงประวัติ Profit/Loss -> ไฟล์จะไปอยู่ที่ `data/export_trade_history.csv`
    -   กด **2**: ดึงกราฟแท่งเทียน (OHLC) -> ไฟล์จะไปอยู่ที่ `data/export_market_data.csv`

---

## ⚙️ 4. การตั้งค่า (`config/settings.py`)

คุณสามารถปรับแต่งบอทได้เอง โดยเข้าไปที่โฟลเดอร์ **`config`** และแก้ไฟล์ **`settings.py`**:

### 💰 Money Management
*   `RISK_DIVISOR`: ตัวหารเงินทุน (10000 = ความเสี่ยงกลางๆ, 5000 = เสี่ยงสูง)

### 🎯 เป้าหมาย (TP / SL)
*   `STOP_LOSS_POINTS`: จุดยอมแพ้ (แนะนำ 400-500)
*   `TAKE_PROFIT_POINTS`: จุดเก็บกำไร (แนะนำ 800-1000)

### 🛑 เป้าหมายรายวัน
*   `DAILY_PROFIT_TARGET`: ถ้ากำไรถึงเป้า ให้หยุดเทรด

---

## ❓ คำถามที่พบบ่อย (FAQ)

**Q: บอทไม่เข้าออเดอร์?**
A: บอทรอกราฟเลือกทาง (Trend Following) บางทีอาจรอเป็นชั่วโมงหรือครึ่งวันครับ ปลอดภัยกว่าเข้ามั่ว

**Q: จะหยุดบอท?**
A: กด `Ctrl` + `C` ที่หน้าจอสีดำ

---
*ขอให้สนุกกับการเทรดครับ! 🚀*
